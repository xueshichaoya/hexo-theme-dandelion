<% if (post.categories && post.categories.length) { %>
    <div class="article-tag">
        <i class="icon czs-tag-l"></i>
        <%- list_categories(post.categories, { show_count: false, style: 'link' }) %>
    </div>
<% } %>
